#include<iostream>
#include<string.h>
#include<unistd.h>
using namespace std;
#define _XOPEN_SOURCE
char *crypt(const char *key,const char *salt);
int main()
{
    int i,j;
    char key[6],Password[10000]="50xcIMJ0y.RXo",cpassword[10000];
    for(i=1;i<=6;i++)
    {
        for(j=0;j<=i;j++)
        {
            
        }
    }
}